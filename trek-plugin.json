{
  "id": "airbnb-mcp",
  "name": "Airbnb Stays",
  "version": "1.8.0",
  "apiVersion": 1,
  "type": "trip-page",
  "trek": ">=4.0.0 <5.0.0",
  "author": "John Jahn",
  "description": "Find somewhere to stay without leaving your trip — search Airbnb for your dates, compare prices and ratings, and add the winner to the plan in one tap.",
  "icon": "BedDouble",
  "license": "MIT",
  "homepage": "https://github.com/jjahn-source/trek-plugin-airbnb-mcp",
  "nativeModules": false,
  "permissions": [
    "db:own",
    "db:meta",
    "db:read:trips",
    "db:write:places",
    "db:write:accommodations",
    "oauth:client",
    "http:outbound:mcp.openbnb.ai",
    "http:outbound:*.muscache.com",
    "http:outbound:server.arcgisonline.com",
    "http:outbound:tile.openstreetmap.org",
    "hook:place-detail-provider"
  ],
  "egress": [
    "mcp.openbnb.ai",
    "*.muscache.com",
    "server.arcgisonline.com",
    "tile.openstreetmap.org"
  ],
  "capabilities": {
    "tripPage": {
      "position": 3
    }
  },
  "settings": [
    {
      "key": "oauth_authorize_url",
      "label": "OAuth authorize URL",
      "input_type": "text",
      "required": true,
      "scope": "instance",
      "placeholder": "https://mcp.openbnb.ai/authorize",
      "hint": "Constant for the hosted OpenBnB service — type the greyed-out value exactly as shown. Change it only if you run your own OpenBnB.",
      "default": "https://mcp.openbnb.ai/authorize"
    },
    {
      "key": "oauth_token_url",
      "label": "OAuth token URL",
      "input_type": "text",
      "required": true,
      "scope": "instance",
      "placeholder": "https://mcp.openbnb.ai/token",
      "hint": "Constant for the hosted OpenBnB service — type the greyed-out value exactly as shown. Change it only if you run your own OpenBnB.",
      "default": "https://mcp.openbnb.ai/token"
    },
    {
      "key": "oauth_client_id",
      "label": "OAuth client id",
      "input_type": "text",
      "required": true,
      "scope": "instance",
      "placeholder": "openbnb-xxxxxxxxxxxxxxxx",
      "hint": "Run `npm run register -- <your TREK URL>` in the plugin repo once, then paste the client id it prints. Unique to this TREK instance."
    },
    {
      "key": "oauth_client_secret",
      "label": "OAuth client secret",
      "input_type": "text",
      "required": true,
      "secret": true,
      "scope": "instance",
      "hint": "From the same command. Stored encrypted; never leaves this server."
    },
    {
      "key": "mcp_url",
      "label": "OpenBnB MCP endpoint",
      "input_type": "text",
      "required": false,
      "scope": "instance",
      "placeholder": "https://mcp.openbnb.ai/mcp",
      "hint": "Optional. Leave blank to use the hosted OpenBnB service; the plugin falls back to the value shown.",
      "default": "https://mcp.openbnb.ai/mcp"
    },
    {
      "key": "map_tile_url",
      "label": "Map tiles",
      "input_type": "text",
      "required": false,
      "scope": "instance",
      "placeholder": "https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Light_Gray_Base/MapServer/tile/{z}/{y}/{x}",
      "hint": "Optional. Leave blank for Esri's grey canvas, which needs no key and matches TREK's own basemap. Point it elsewhere and add that host under Allowed hosts.",
      "default": "https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Light_Gray_Base/MapServer/tile/{z}/{y}/{x}"
    },
    {
      "key": "map_tile_url_dark",
      "label": "Map tiles (dark theme)",
      "input_type": "text",
      "required": false,
      "scope": "instance",
      "placeholder": "https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Dark_Gray_Base/MapServer/tile/{z}/{y}/{x}",
      "hint": "Optional. Leave blank for Esri's dark grey canvas, used automatically on a dark theme.",
      "default": "https://server.arcgisonline.com/ArcGIS/rest/services/Canvas/World_Dark_Gray_Base/MapServer/tile/{z}/{y}/{x}"
    },
    {
      "key": "map_attribution",
      "label": "Map attribution",
      "input_type": "text",
      "required": false,
      "scope": "instance",
      "placeholder": "Tiles © Esri — Esri, DeLorme, NAVTEQ",
      "hint": "Optional. Leave blank and the credit follows whichever tile source is in use. Set it if you point the tiles at a provider the plugin does not recognise.",
      "default": "Tiles © Esri — Esri, DeLorme, NAVTEQ"
    }
  ],
  "actions": [
    {
      "key": "test_connection",
      "label": "Test connection",
      "hint": "Checks this instance’s OpenBnB settings end to end and names the first thing that is wrong."
    }
  ],
  "operatorEgress": true
}
